<resources>

    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>

        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:background">@color/colorBackground</item>
    </style>

    <style name="newsDigestText" >
        <item name="android:textColor">@color/colorText</item>
    </style>
    <style name="newsListTitleText" parent="Widget.AppCompat.ListView">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:textSize">@dimen/newsListTitleSize</item>
    </style>

    <style name="newsListUnreadCounter" parent="Widget.AppCompat.ListView">
    <item name="android:textColor">@color/colorText</item>
        <item name="background">@color/counterBackground</item>
    <item name="android:textSize">@dimen/newsListTitleSize</item>
    </style>
    <style name="newsListDescriptionText" parent="Widget.AppCompat.ListView">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:textSize">@dimen/newsListDescriptionSize</item>
    </style>
    <style name="newsListTimeText" parent="Widget.AppCompat.ListView">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:textSize">@dimen/newsListTimeSize</item>
    </style>

    <style name="toolbarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/colorBackground</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="titleTextColor">@color/colorText</item>
        <item name="colorControlNormal">@color/colorText</item>
    </style>

    <style name="navigationView" >
        <item name="android:textColor">@color/colorText</item>
        <item name="android:background">@color/colorBackground</item>
    </style>

    <style name="AlertDialogCustom" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <item name="android:textColorPrimary">@color/colorText</item>
<!--        <item name="colorAccent">@color/colorText</item>-->
<!--        <item name="android:background">@color/colorBackground</item>-->
    </style>
    <style name="MyDarkToolbarStyle" parent="@style/Widget.AppCompat.Spinner.DropDown.ActionBar">
        <item name="background">?android:selectableItemBackground</item>
        <item name="android:popupBackground">@color/colorBackground</item>
        <item name="android:dropDownSelector">?android:selectableItemBackground</item>
        <item name="android:divider">@color/colorText</item>
        <item name="android:overlapAnchor">true</item>
    </style>
</resources>
