<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:padding="@dimen/setup_source_item_padding">

    <ImageView
        android:id="@+id/source_setup_icon_view"
        android:layout_width="@dimen/source_icon_settings"
        android:layout_height="@dimen/source_icon_settings"
        android:layout_margin="@dimen/setup_source_item_padding"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingStart="@dimen/setup_source_item_padding">

        <TextView
            android:id="@+id/source_title"
            style="@style/newsListTitleText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/source_url"
            style="@style/newsListDescriptionText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </LinearLayout>
</LinearLayout>